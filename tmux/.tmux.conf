set-option -g default-shell /usr/bin/zsh

# Set Alt as prefix key
unbind C-b
set-option -g prefix M-a
bind-key M-a send-prefix

# Start windows and panes at 1, not 0
set -g base-index 0
set -g pane-base-index 1
set -g default-terminal "tmux-256color"
set -ga terminal-overrides ",alacritty:Tc"
set-window-option -g pane-base-index 1
set-option -g renumber-windows on

# Window switching with Alt+0-9
bind-key -n M-0 select-window -t 0
bind-key -n M-1 select-window -t 1
bind-key -n M-2 select-window -t 2
bind-key -n M-3 select-window -t 3
bind-key -n M-4 select-window -t 4
bind-key -n M-5 select-window -t 5
bind-key -n M-6 select-window -t 6
bind-key -n M-7 select-window -t 7
bind-key -n M-8 select-window -t 8
bind-key -n M-9 select-window -t 9

# Vim-style pane navigation
bind-key -n M-h select-pane -L
bind-key -n M-j select-pane -D
bind-key -n M-k select-pane -U
bind-key -n M-l select-pane -R

# Vim-style pane splitting
bind-key -n M-v split-window -h -c "#{pane_current_path}"
bind-key -n M-s split-window -v -c "#{pane_current_path}"

# Pane resizing with vim keys
bind-key -n M-H resize-pane -L 5
bind-key -n M-J resize-pane -D 5
bind-key -n M-K resize-pane -U 5
bind-key -n M-L resize-pane -R 5

# Window management
unbind -n M-c                                        # freed: Alt+C passes to the shell (fzf cd-widget)
bind-key -n M-n new-window -c "#{pane_current_path}" # new window/tab (no-prefix)
bind-key c     new-window -c "#{pane_current_path}"  # ...and on prefix+c
bind-key -n M-x kill-pane
bind-key -n M-X kill-window

# Session management
unbind -n M-d #unbind alt-d to detach
bind-key -n M-F12 detach-client
bind-key -n M-r command-prompt -I "#S" "rename-session '%%'"
bind-key -n M-N new-session                          # Alt+Shift+N (Alt+N is new-window)

# Quick session switching (Alt+Tab style)
bind-key -n M-Tab switch-client -l
bind-key -n M-BTab switch-client -p

# Copy mode with vim keys
set-window-option -g mode-keys vi
bind-key -n M-[ copy-mode
bind-key -n M-m copy-mode
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle
bind-key -T copy-mode-vi y     send-keys -X copy-pipe-and-cancel "win32yank.exe -i --crlf"
bind-key -T copy-mode-vi Enter send-keys -X copy-pipe-and-cancel "win32yank.exe -i --crlf"
bind-key -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel "win32yank.exe -i --crlf"

# Clipboard via win32yank (direct WSL->Windows interop), NOT OSC 52 through the
# terminal. Emitting OSC 52 to Windows Terminal intermittently blocks while WT
# acquires the Windows clipboard lock; that backpressures ConPTY and freezes
# tmux's single-threaded server (selection stays highlighted for seconds, then
# snaps back). set-clipboard must be off, or tmux still emits OSC 52 alongside
# the pipe and the freeze returns. win32yank.exe lives in ~/.local/bin (on PATH).
set -g set-clipboard off

# Mouse support
set -g mouse on

# Reload config
bind-key -n M-R source-file ~/.tmux.conf \; display-message "Config reloaded!"

# Better colors
set -g default-terminal "screen-256color"
set -ga terminal-overrides ",*256col*:Tc"

# Pane outline
set -g pane-active-border-style 'fg=colour208,bg=default'
set -g pane-border-style 'fg=colour238,bg=default'

# Status bar
set -g status-position top
set -g status-bg colour234
set -g status-fg colour137
set -g status-left ''
set -g status-right '#[fg=colour233,bg=colour241,bold] %d/%m #[fg=colour233,bg=colour245,bold] %H:%M:%S '
set -g status-right-length 50
set -g status-left-length 20

# Window status
setw -g window-status-current-format ' #I#[fg=colour250]:#[fg=colour255]#W#[fg=colour50]#F '
setw -g window-status-format ' #I#[fg=colour237]:#[fg=colour250]#W#[fg=colour244]#F '

# Escape time for vim
set -sg escape-time 0

# History limit
set -g history-limit 10000
